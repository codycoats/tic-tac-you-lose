name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Deploy to gh pages
      run: |
        git config --global user.email "gh-bot@codycoats.com"
        git config --global user.name "Deploy Bot"
        yarn && yarn deploy
